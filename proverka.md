# Самопроверка каталога

Дата: 21.07.2026. Проверено вручную (открыл в браузере) и программно (скрипт-измеритель высоты
каждого экрана + grep по всему репозиторию).

## Конфиденциальность

- [x] Нигде нет названия производителя, его сайта, ссылок на него.
      Проверено: `grep -inE "tver|ecostroi|ТверьЭкоСтрой" index.html build_catalog.py *.md` — чисто.
- [x] В репозитории нет файлов прайса, сверки, разведки.
      Проверено: `git ls-files` — только dannye.md/teksty.md/voprosy.md (производные, без опта
      и без URL поставщика) и файлы вёрстки.

## Работоспособность

- [x] Кнопка «Позвонить» открывает набор номера — `tel:+74950855990`, номер на кнопке не написан.
- [x] Все три мессенджера открывают диалог, иконки настоящие и цветные — WhatsApp/Telegram с
      cdn.simpleicons.org (официальные бренд-SVG), Max — с фавикона самого max.ru (в Simple Icons
      этого мессенджера нет). Все три — локальные файлы в foto/icons/, без ссылок на CDN.
- [x] Каждая строка навигатора проваливается на нужный экран — проверено программно (парсинг
      отрендеренного DOM): все 20 строк навигатора ведут на реально существующие id экранов.
- [x] Ссылка «↑ к навигатору» работает на каждом экране — проверено программно: присутствует на
      всех 52 экранах, кроме 6 экранов-разделителей (они полноцветные, без подвала — так и
      задумано в концепции).

## Вёрстка

- [x] Все экраны 1080×1920, ничего не выходит за края — проверено программно (скрипт меряет
      реальную высоту содержимого каждого из 52 экранов, временно отключив overflow:hidden):
      максимум 1920px, переполнений нет. По ходу нашёл и исправил 4 реальных переполнения
      (см. voprosy.md) до сдачи, не после.
- [x] Слоты под фото точного размера, заглушки подписаны именами файлов — имена в заглушках
      совпадают с foto/PLACEHOLDER.txt (сверено списком, 69 файлов, полное совпадение).
- [x] При печати в PDF каждый экран — отдельная страница — проверено: `--print-to-pdf` даёт
      ровно 52 страницы.
- [x] Цифры в таблицах моноширинные и выровнены по разрядам — PT Mono, `toLocaleString('ru-RU')`.
- [x] Подчёркиваний внутри текста нет — `grep -c "<u>" index.html` = 0.
- [x] Отступы, высота шапки и положение кнопок одинаковы на всех экранах — шапка и подвал
      собираются одной и той же JS-функцией (`headerBlock`/`footerBlock`) на всех экранах, кроме
      разделителей разделов (они по концепции полноцветные, без шапки/подвала).

## Данные

- [x] Все цены пересчитаны как прайс поставщика плюс 5% с округлением вверх — с 4 исключениями,
      по прямому указанию штаба на этапе 1: 4 цены сведены к уже опубликованным объявлениям
      (округление до ближайшего, не строго вверх), чтобы каталог не разошёлся с рекламой.
      Разница зафиксирована и согласована, не самовольная.
- [x] Цены на бани взяты готовыми, не пересчитывались — источник тот же файл, что и печатный
      банный каталог (`catalog_data.py`), сверено штабом «до рубля».
- [x] Ни одна модель из перечисленных семейств не потеряна — 159 моделей по 18 семействам
      (163 в исходном прайсе минус 4: 3 модели линейки «с верандой» и 1 беседка со сборкой на
      месте — все сознательно исключены как «сборка на месте», список в dannye.md).
- [x] Раздела «Строительство домов» в каталоге нет — не включён ни на одном этапе.

## Итог

Каталог собран: 52 экрана (5 входных + 6 разделителей + 34 экрана по 17 семействам «визуальный
+ данные» + 1 экран «Малые формы» одним листом + 6 заключительных). Все проверки выше пройдены.
Фотографий пока нет нигде — везде подписанные серые заглушки, список для владельца в
foto/PLACEHOLDER.txt.
